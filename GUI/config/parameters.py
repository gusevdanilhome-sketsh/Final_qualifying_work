#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Модуль конфигурации параметров системы.
Обновлено для соответствия таблице распределения классов.
"""

import numpy as np

# Физические константы
C_LIGHT = 3e8  # Скорость света, м/с

# Параметры микрополосковой линии
# Примечание: согласно таблице, общая длина сканирования = 10 мм
LINE_PARAMS = {
    'width': 0.003,           # Ширина проводника W = 3 мм
    'height': 0.0005,         # Толщина подложки h = 0.5 мм
    'thickness': 0.000035,    # Толщина проводника t = 35 мкм
    'epsilon_r': 4.5,         # Относительная диэлектрическая проницаемость
    'length': 0.010,          # Длина линии L = 10 мм (согласно таблице)
    'z0_nom': 50.0            # Номинальное волновое сопротивление, Ом
}

# Параметры дефектов (согласно таблице распределения)
DEFECT_PARAMS = {
    'segment_length': 0.001,  # Длина сегмента = 1 мм (10 точек по 0.1 мм)
    'scanning_step': 0.0001,  # Шаг сканирования = 0.1 мм (согласно таблице)
    'severity_range': (0.5, 0.8),  # Диапазон выраженности дефекта
    'defect_types': [1, 2, 3, 4],  # Типы дефектов (1-4), 0 - без дефекта
    'n_segments': 10          # Количество сегментов (согласно таблице)
}

# Параметры измерительной головки
PROBE_PARAMS = {
    'a': 0.003,               # Сторона квадрата = 3 мм (совпадает с шириной МПЛ)
}

# Параметры зондирования (VKR.docx: 1-10 ГГц)
FREQ_PARAMS = {
    'frequencies': np.linspace(1e9, 10e9, 10),  # 10 частот от 1 до 10 ГГц
    'power': 1.0              # Мощность зондирующего сигнала, Вт
}

# Параметры шумовой модели (VKR_V2.docx раздел 4)
NOISE_PARAMS = {
    'snr_db': 40,             # Отношение сигнал/шум, дБ
    'seed': 42                # Сид определяет порядок типов дефектов
}

# Параметры выборки (VKR.docx раздел 1.3.2)
DATASET_PARAMS = {
    'n_samples_per_segment': 10,  # Образцов на сегмент (согласно таблице)
    'classes': 5,                # 5 классов дефектов
    'test_size': 0.3,            # Доля тестовой выборки
    'random_state': 42           # Сид для разделения
}

# Параметры вывода
OUTPUT_PARAMS = {
    'base_dir': 'output',
    'data_dir': 'output/data',
    'figures_dir': 'output/figures',
    'reports_dir': 'output/reports'
}

# Названия классов дефектов (VKR.docx раздел 1.1.2)
CLASS_NAMES = [
    'Без дефекта',
    'Утонение высоты',
    'Утонение ширины',
    'Утонение подложки',
    'Изменение εr'
]

# Цвета для классов дефектов (для визуализации)
CLASS_COLORS = ['black', 'red', 'blue', 'green', 'orange']

# Последовательность сегментов согласно таблице
# 0=без дефекта, 1-4=типы дефектов
SEGMENT_SEQUENCE = [0, 1, 0, 2, 0, 1, 0, 3, 0, 4]